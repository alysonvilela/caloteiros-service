service: caloteiros-service
frameworkVersion: '3'

plugins:
  - serverless-esbuild
  - serverless-offline
  - serverless-offline-watcher

custom:
  useDotenv: true
  serverless-offline:
    httpPort: 4000
  serverless-offline-watcher:
    - path:
        - src/**/*.ts
      command: echo "Getted an update, refreshing..."

provider:
  name: aws
  runtime: nodejs18.x

package:
  individually: true

functions:
  send:
    handler: src/functions/service-owner-controller.handler
    events: 
      - http:
          path: send
          method: post
          
