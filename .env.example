# PostgreSQL Database Configuration
# For Docker Compose, these are optional (defaults are provided)
# For production, replace with your actual database credentials
PGHOST=postgres
PGDATABASE=caloteiros_db
PGUSER=caloteiros
PGPASSWORD=caloteiros123
ENDPOINT_ID=local

# WhatsApp API Configuration (handled by docker-compose)
# WHATSAPP_BASE_URL is automatically set in docker-compose

# Node Environment
NODE_ENV=development

# Note: When using Docker Compose, you can leave these as defaults
# The docker-compose.yml file provides sensible defaults for local development